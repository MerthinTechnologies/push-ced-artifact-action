export declare class CreateVersionCommandHandler {
    private _configService;
    private _versionService;
    constructor(token?: string);
    run(environment?: string, rawOutput?: boolean): Promise<string>;
    private _createVersion;
}
