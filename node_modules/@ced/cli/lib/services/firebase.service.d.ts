import * as firebase from 'firebase/app';
import 'firebase/auth';
import 'firebase/storage';
export declare class FirebaseService {
    constructor();
    login(token: string): Promise<firebase.auth.UserCredential>;
}
