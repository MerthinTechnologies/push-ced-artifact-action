import { AuthConfig } from './auth-config.service';
export declare class AuthService {
    private _configService;
    constructor();
    startLogin(): Promise<string>;
    waitForToken(requestToken: string): Promise<string>;
    private _sleep;
    logout(): Promise<void>;
    setTokens(requestToken: string, token: string): void;
    getTokens(): AuthConfig | undefined;
    getTokenOrThrow(defaultToken?: string): string;
    private _getToken;
}
