export declare class ArtifactsService {
    private _firebaseService;
    private _securityService;
    private _organizationService;
    constructor(token?: string);
    initialize(projectId: string): Promise<void>;
    upload(projectId: string, environment: string, subsystemId: string, versionId: string, fileName: string, progressCallback?: (progress: number) => void | undefined, completionCallback?: (downloadUrl: string) => void | undefined): Promise<void>;
    private _upload;
}
