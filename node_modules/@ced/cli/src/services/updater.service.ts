import * as updateNotifier from "update-notifier";

export class UpdaterService {

  public static notifyAvailableUpdate() {
    const pkg = require('../../package.json');
    const notifier = updateNotifier({
      pkg
    });
    notifier.notify();
  }
}